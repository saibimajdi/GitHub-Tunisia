@model List<Octokit.Repository>
@{
    ViewBag.Title = "Top Tunisian Repos";
}
<h2>@ViewBag.Title.</h2>


<div class="row">
    <center>
        <h1>
            <span class="glyphicon glyphicon-alert" style="font-size:150px;"></span>
        </h1>
        <h3>
            Top Repos will be available soon, thank you for your patience!
        </h3>
    </center>
</div>


@*<div class="row">
    <h3>Top tunisian repositories in GitHub!</h3>
    <div class="col-md-12">
        <table class="table">
            <thead>
                <tr>
                    <th>#</th>
                    <th></th>
                    <th>name</th>
                    <th>email</th>
                    <th>bio</th>
                    <th>blog</th>
                    <th>followers</th>
                </tr>
            </thead>
            <tbody>
                @{ var counter = 1; }
                @foreach (var repo in Model)
                {
                    <tr>
                        <td>
                            <b>@counter</b>
                        </td>
                        <td>
                            <img src="@repo.AvatarUrl" style="height:40px;width:40px;" class="img-circle" />
                        </td>
                        <td>
                            <a href="@repo.HtmlUrl">@(repo.Name ?? (repo.Login ?? "N/A"))</a>
                        </td>
                        <td>
                            @(repo.Email ?? "N/A")
                        </td>
                        <td>
                            <i class="text-muted">@(repo.Bio ?? "...")</i>
                        </td>
                        <td>
                            <a href="@(repo.Blog ?? "#")">@(repo.Blog ?? "N/A")</a>
                        </td>
                        <td>
                            @(repo.Followers)
                        </td>
                    </tr>
                    // increasing the counter
                    counter++;
                }
            </tbody>
        </table>
    </div>
</div>*@
