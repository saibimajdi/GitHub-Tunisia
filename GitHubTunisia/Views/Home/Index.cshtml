@model List<Octokit.User>
@{
    ViewBag.Title = "Home Page";

    var bestCoder = Model.FirstOrDefault();
    Model.RemoveAt(0);

}

<div class="jumbotron" style="display: inline-block;width: 100%;">
    <div class="col-md-8">
        <h1>GitHub Tunisia</h1>
        <p class="lead">Numbers about the tunisian coders in <a href="https://github.com">GitHub</a>!</p>
    </div>
    <div class="col-md-4">
        <img src="~/Content/logo.png" style="width:180px; border-radius:90px;" />
    </div>
</div>

<div class="row">
    <h3>Best tunisian coders in GitHub!</h3>
    <div class="col-md-12">
        <div class="col-md-4"></div>
        <div class="col-md-4">
            <center style="background-color: darkslategrey;border-radius: 90px;padding: 20px;margin: 10px;margin-bottom: 40px;">
                <img src="Content/ribbon.png" style="height: 60px;position: absolute;margin-top: -10px;" />
                <img style="width:100px; height:100px" src="@bestCoder.AvatarUrl" alt="@(bestCoder.Name?? bestCoder.Login)" class="img-circle" />
                <h3 style="color:goldenrod;font-style: italic;font-weight: 900;text-transform: uppercase;text-shadow: 1px 1px 5px goldenrod;">
                    <a href="@bestCoder.HtmlUrl" style="text-decoration: none;color: goldenrod;">@(bestCoder.Name ?? bestCoder.Login)</a>
                </h3>
                <p>
                    @bestCoder.Bio
                </p>
            </center>
        </div>
        <div class="col-md-4"></div>
    </div>
    <div class="col-md-12">
        <table class="table">
            <thead>
                <tr>
                    <th>#</th>
                    <th></th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Bio</th>
                    <th>Blog</th>
                    <th>Followers</th>
                </tr>
            </thead>
            <tbody>
                @{ var counter = 2; }
                @foreach(var user in Model)
                {
                    <tr>
                        <td>
                            <b>@counter</b>
                        </td>
                        <td>
                            <img src="@user.AvatarUrl" style="height:40px;width:40px;" class="img-circle" />
                        </td>
                        <td>
                            <a href="@user.HtmlUrl">@(user.Name ?? (user.Login ?? "N/A"))</a>
                        </td>
                        <td>
                            @(user.Email ?? "N/A")
                        </td>
                        <td>
                            <i>@(user.Bio ?? "...")</i>
                        </td>
                        <td>
                            <a href="@(user.Blog ?? "#")">@(user.Blog ?? "N/A")</a>
                        </td>
                        <td>
                            @(user.Followers)
                        </td>
                    </tr>
                    // increasing the counter
                    counter++;
                }
            </tbody>
        </table>
    </div>
</div>